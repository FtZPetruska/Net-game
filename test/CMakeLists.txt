cmake_minimum_required(VERSION 3.10)

enable_testing()

include_directories(${NET_HEADERS_DIR})

add_executable(tests tests.c)
target_link_libraries(tests game)

add_test(new_game               tests   new_game)
add_test(game_height            tests   game_height)
add_test(rotate_piece           tests   rotate_piece)
add_test(is_edge                tests   is_edge)
add_test(get_piece              tests   get_piece)
add_test(is_game_over           tests   is_game_over)
add_test(set_piece              tests   set_piece)
add_test(game_width             tests   game_width)
add_test(set_piece_current_dir  tests   set_piece_current_dir)
add_test(opposite_dir           tests   opposite_dir)
add_test(delete_game            tests   delete_game)
add_test(restart_game           tests   restart_game)
add_test(new_game_empty_ext     tests   new_game_empty_ext)
add_test(new_game_ext           tests   new_game_ext)
add_test(new_game_empty         tests   new_game_empty)
add_test(shuffle_dir            tests   shuffle_dir)
add_test(rotate_piece_one       tests   rotate_piece_one)
add_test(is_edge_coordinates    tests   is_edge_coordinates)
add_test(copy_game              tests   copy_game)
add_test(get_current_dir        tests   get_current_dir)
add_test(is_wrapping            tests   is_wrapping)